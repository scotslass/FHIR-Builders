# validation.cfg — default paths for the validation test suite.
# Read by tests/conftest.py. Gitignored copies may override these locally.

[validation]
# Path to a newline-delimited JSON (.ndjson) or JSON Bundle of FHIR resources
# to evaluate, and where the resulting report should be written.
input_resources =
output_report =
